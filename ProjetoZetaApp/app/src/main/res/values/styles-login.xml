<resources>

    <!-- Base application theme. -->
    <style name="styleLogin" parent="Theme.AppCompat.Light">
        <item name="android:windowBackground">@color/colorTema</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

</resources>
